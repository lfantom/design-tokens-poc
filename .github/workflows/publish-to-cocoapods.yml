name: "Publish to CocoaPods for iOS"

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  build:
    runs-on: macos-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: List Directory Contents
        run: ls -R
        working-directory: ${{ github.workspace }}

      - name: Install Bundler
        run: gem install bundler

      - name: Bundle Install
        run: bundle install
        working-directory: build/app/ios/
        env:
          BUNDLE_GEMFILE: .gemfile

      - name: Set Version
        id: version
        run: |
          echo "VERSION=$(git describe --tags --abbrev=0)" >> $GITHUB_ENV
          echo "VERSION=${VERSION:1}" >> $GITHUB_ENV
        shell: bash

      - name: Update Podspec Version
        run: |
          cat build/app/ios/DesignTokensiOS.podspec
          sed -i '' "s/s.version *= *'[0-9]*\.[0-9]*\.[0-9]*'/s.version = '$(echo ${VERSION})'/" build/app/ios/DesignTokensiOS.podspec

      - name: Debug - Print Podspec Content
        run: cat build/app/ios/DesignTokensiOS.podspec

      - name: Debug - Print Sed Result
        run: sed -i '' "s/s.version *= *'[0-9]*\.[0-9]*\.[0-9]*'/s.version = '$(echo ${VERSION})'/" build/app/ios/DesignTokensiOS.podspec

      - name: Commit and Push Changes
        run: |
          git config --local user.email "uxdev@jet2.com"
          git config --local user.name "GitHub Action"
          git status
          git add .
          git status
          git commit -m "Bump version to ${VERSION} in DesignTokensiOS.podspec"
          git push origin HEAD

      - name: Lint CocoaPods
        run: bundle exec pod lib lint DesignTokensiOS.podspec

      - name: Push to CocoaPods
        run: bundle exec pod trunk push --allow-warnings
        env:
          COCOAPODS_PODSPEC_NAME: DesignTokensiOS.podspec
